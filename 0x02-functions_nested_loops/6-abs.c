#include "main.h"
/**
 * _abs(int - copies the absolute of an integer
 * @abs: function parameter
 * Return: ((a -a)
 */
int _abs(int)
{
	if (a < 0)
		return (-a):
	else if (a >= 0)
	{
		return (a);
	}
}
